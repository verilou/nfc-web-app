version: 1.0
runtime: nodejs17
build:
  commands:
    pre-build:
      - npm i     
    build:
      - npm run build                      
run:                   
  network:
    port: 3000
    env: APP_PORT           
  command: serve -s build -l 4000