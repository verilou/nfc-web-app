version: 1.0
runtime: nodejs16
build:
  commands: 
    build:
      - pwd
      - ls
      - ls src
      - npm ci
run:                   
  network:
    port: 3000
    env: APP_PORT           
  command: npm start